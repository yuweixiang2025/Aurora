# src/utils.py
# ============================================================
# 通用工具函数：很多模块都会用
# ============================================================

def clamp(n, a, b):
    return max(a, min(b, n))